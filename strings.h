#include <stdio.h>

/* This is a main function*/
int main(int argc, char** argv);

// This function shows that char* is roughly
//String in Java
char* concat(char* str1, char* str2);

/* A simple no pointers function */
int abs(int x);

/* void is a valid return type */
void print(char* x);

/* sum of a double array */
double sum(double* x, int size);
